

{% extends "module-form-settings.twig" %}
{% import "forms.twig" as forms %}

{% block moduleFormFields %}

    {% set title %}{% trans "Update Interval" %}{% endset %}
    {% set helpText %}{% trans "The number of seconds after which player will remove HTML Package files from its local library after they are no longer required" %}{% endset %}
    {{ forms.number("updateInterval", title, module.getSetting("updateInterval", 259200), helpText) }}

{% endblock %}